name: CheckPayload

on:
  issues:
    types:
      - labeled
    labels:
      - '동작 안시킬꺼임'

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Show event payload
        run: cat $GITHUB_EVENT_PATH

## 제이슨 형식으로 정리해서 볼 수 있음
